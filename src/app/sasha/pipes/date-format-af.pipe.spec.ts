import { DateFormatAfPipe } from './date-format-af.pipe';

describe('DateFormatAfPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatAfPipe();
    expect(pipe).toBeTruthy();
  });
});
